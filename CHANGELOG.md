# 更新日志

本文件记录每个已发布版本的变更。

**发布流程强制依赖它**：`tools/release-apk.mjs` 会从这里提取对应版本的段落，
作为 GitHub Release 的说明；**提取不到就直接拒绝发布**。所以每发一版，
必须先把这一版改了什么写在这里，而不是等发完再补。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
**已发布的版本一律保留**，即使有问题也只在条目标注，不删除 ——
别人可能已经装了，撤掉版本号会让「我装的是哪一版」无从查起。

---

## [1.2.8] - 2026-09-18

> **这是自 v1.2.0 以来的第一个正式发布。**
> 1.2.1 ~ 1.2.7 都是围绕「切到校园跑就读到真实位置」这一个问题的调试版，没有单独发布，
> 但它们各自的条目都保留在下面，可以看清问题是怎么一步步定位的。
>
> 从 v1.2.0 升级上来的完整变化：
>
> - **修好了模拟位置在校园跑类应用里失效** —— 真机心跳数据证明原因是
>   **进程被 MIUI 冻结了 137 秒**（145 秒的会话里只有约 8 秒真正在推送），
>   而不是被反作弊识别；配套加了保活机制与后台设置引导。
> - 新增**定位诊断**（挂载了哪些 provider、当前值、是否 [模拟]）与**后台存活心跳**。
> - 补上 `ACCESS_BACKGROUND_LOCATION`，推送频率 5 Hz → 10 Hz，
>   provider 属性与 provider 集合逐项对齐影梭（ZCShou/GoGoGo）。
> - 自动化「停止模拟 → 再开始模拟」这个被验证有效的操作。
> - 删掉一处实测无效、每秒空跑 20 次的反射开销。
> - 新增首次启动的**使用须知与免责声明**关卡。

> 模拟位置已经能稳定生效（1.2.6 修好后台冻结、1.2.7 自动化重挂 provider）。
> 这一版做两件事：清掉一处实测无效却一直在跑的开销，以及把使用限制变成应用内的显式关卡。

### 优化

- **删除每帧的反射调用**：`MockLocationEngine` 里原有一段用
  `Location.setMock(false)`（API 31 前叫 `setIsFromMockProvider`）抹除模拟标记的代码。
  删除依据是真机数据而不是判断：

  1. 诊断显示全部 provider 都标着 `[模拟]`，而模拟位置在目标应用里**已经能正常生效** ——
     说明对方根本不看这个标记，当初的猜测是错的；
  2. 反射虽然「调用成功」，读回来仍是 `[模拟]`：系统收到 `setTestProviderLocation()`
     之后会强制重新打标记，客户端改不掉；
  3. 代价却是实打实的：`getDeclaredMethod` + `invoke` 每个 provider 每帧各一次，
     10 Hz × 2 个 provider = **每秒 20 次反射查找**，全程零收益。

  结论本身保留在注释里（免 root 的模拟位置必定带 `[模拟]` 且改不掉），
  但不再有代码去尝试伪装它。

### 新增

- **首次启动的「使用须知与免责声明」关卡**：必须点掉确认按钮才能进入主界面，
  放在口令页**之前**。核心内容一句话 —— **本应用不建议且不可用于作弊，
  而且技术上也不成立**（模拟标记改不掉，任何做基本校验的应用都能识别），
  并明确列出禁止用途与责任归属。
- 「更多工具 → 使用须知」可随时重看同一份内容（非关卡模式，可直接关闭）。
- README 顶部新增同等分量的「使用限制与免责声明」，替换掉原先那段语气偏弱的提醒。

### 变更

- 诊断文本重排并改写了读法说明。原先把「模拟标志」放在最前面，
  会让人以为它是关键指标 —— 实测恰恰相反：
  - `[模拟]` **不代表有问题**；
  - `最后位置：无` 或某个 provider 显示 `[真实]` **也不代表失效**
    （`getLastKnownLocation()` 读的是各 provider 自己的缓存，而模拟值是实时推给监听者的）；
  - 真正决定成败的是「后台存活心跳」那一段，现在它排在最前并直接给结论。
- 版本号 1.2.7 → 1.2.8

---

## [1.2.7] - 2026-09-18

### 修复

- **把「停止模拟 → 再开始模拟」这个有效操作自动化了。**

  1.2.6 之后模拟位置已经能生效，但用户反馈了一个很具体的时序问题：

  | 操作顺序 | 结果 |
  | --- | --- |
  | 先开始模拟 → 再打开校园跑 | **不生效** |
  | 校园跑已在运行时，把模拟停止再开始 → 切回校园跑 | **生效** |
  | 之后再切回本应用 → 又切回校园跑 | 再次失效 |

  也就是说，「重新挂载 provider」这个动作本身能让对方开始读到模拟值。

  ⚠️ **为什么有效，我没有确认。** 写成「重新挂载 provider 就能生效」是**事实记录**，
  不是机制解释 —— 与其继续猜系统内部行为，不如先把一条被验证有效的操作自动化。
  具体实现见 `MockLocationEngine.remount()` 的注释。

### 新增

- `MockLocationEngine.remount()`：把已挂载的 provider 全部先 `removeTestProvider`
  再 `addTestProvider`，并立刻补推一帧，避免重挂瞬间出现空档。
- **界面转入后台时立即重挂一次**（`MainActivity.onStop` → `MockLocationService.onUiBackground`）。
  这一步对应上表第二行：用户离开本应用的那一刻，正是他要切去校园跑的时刻。
- **后台每 30 秒兜底重挂一次**，界面回到前台即停止 —— 重挂会让 provider 有一瞬间空档，
  对方可能看成一次「GPS 信号丢失」，所以不在用户操作时打扰系统，间隔也不压得太短。

### 变更

- 版本号 1.2.6 → 1.2.7

---

## [1.2.6] - 2026-09-18

> 1.2.5 的后台心跳拿到了决定性证据，这一版是照着那份数据改的。

### 修复

- **定位到「切到校园跑就读到真实位置」的真正原因：进程被系统冻结，不是被反作弊识别。**

  1.2.5 在真机上的一次 2 分 25 秒模拟会话，心跳记录如下：

  ```
  本次已运行：2 分 25 秒
  累计推送：166 帧，实际 1.1 帧/秒（正常应为 20 帧/秒）
  最长心跳间隔：137.3 秒
    · 22:22:23 起冻结 137.3 秒
  ```

  用户从本应用切到步道乐跑的那一刻，进程被 MIUI 冻结了 137 秒，
  直到切回来才解冻 —— 整场 145 秒里只有约 8 秒真正在推送。
  那段时间系统 provider 收不到任何模拟值，对方读到的只能是真实定位。

- 心跳的「中断」门槛由 1000ms 改成 2500ms。
  心跳本来就是每秒落一次，旧门槛会把每一次正常心跳都记成「冻结 1.0 秒」，
  真正出问题的那一次反而被噪声淹没（1.2.5 的日志里就有三条这种误报）。

### 新增

- **保活机制**（`KeepAlive.kt`），在开始模拟时启动、结束时释放：
  - `PARTIAL_WAKE_LOCK`：避免进程在熄屏/Doze 时被挂起；
  - 一个 1×1 的不可见常驻窗口（需「显示悬浮窗」权限）：
    抬高进程优先级，这是与影梭最结构性的一处差异 —— 它常驻一个摇杆悬浮窗。
    **这一条是推测性的**，拿不到权限就不加窗口，不影响其他功能。
- **「保活设置」菜单**：把三个系统设置入口直接摆到界面上，并实时显示哪一项还没打开 ——
  电池优化白名单、自启动、显示悬浮窗。MIUI 把这些开关藏在三四个不同的地方。
  （应用无权给自己开白名单，只能引导。）
- 诊断里新增「系统限制状态」段落，直接列出还没打开的限制项；
  心跳段落末尾给出一句**有依据的**结论，而不是让用户自己对着数字猜。

### 变更

- 诊断中「累计推送」改为显示实际帧率与期望帧率（20 帧/秒 = 2 个 provider × 10 Hz），
  并据此估算「有多长时间没在推送」。
- Manifest 增加 `SYSTEM_ALERT_WINDOW`。
- 版本号 1.2.5 → 1.2.6

---

## [1.2.5] - 2026-09-18

> 这一版集中处理「在高德地图、小米运动健康里模拟位置有效，
> 但在校园跑类 App 里仍显示真实位置」的问题。1.2.1 ~ 1.2.4 都是围绕它的调试版，
> 没有单独发布，下面把自 1.2.0 以来的改动一并写清楚。

### 新增

- **定位诊断**（右上角菜单 → 定位诊断）：把以前只写在 logcat 里的信息摊到界面上 ——
  本应用挂载了哪些 provider、系统里每个 provider 当前是什么值、是否被标记为 `[模拟]`。
  一段文字可直接复制。
- **后台存活心跳**：记录本次模拟已运行多久、累计推送了多少帧、
  **最长推送间隔**是多少，以及后台被冻结的具体时刻。
  它写在 SharedPreferences 里，所以进程被杀之后最后一个心跳仍然留得下来。
  这一条是为了把三种长得一模一样的失败原因分开：
  服务被杀 / 服务被冻结 / 服务一直正常但对方仍不用 —— 前两种能修，第三种是免 root 的边界。
- 诊断对话框里新增「省电白名单」按钮，一键跳转系统电池优化授权页。

### 变更

- **模拟位置改为逐项对齐影梭（ZCShou/GoGoGo）**：只挂 `gps` 与 `network` 两个 provider
  （原先还挂了 `fused`、`passive`），provider 属性也照抄它 ——
  `gps` 是 `requiresSatellite=true` / 精度 `FINE` / 功耗 `HIGH`，
  `network` 是 `requiresNetwork=true` / `requiresCell=true` / 精度 `COARSE`。
  这些属性决定系统用 `Criteria` 匹配定位源时能不能选中它。
- 推送频率 5 Hz → 10 Hz（对齐影梭），减少真实定位插入空档的机会。
- 写入位置时附带 `satellites=7`（对齐影梭）。
- Manifest 补上 `ACCESS_BACKGROUND_LOCATION`。
- 复制诊断内容时不再漏掉权限状态（此前只复制了 provider 那一段）。

### 说明

- 免 root 修改位置只能走 `addTestProvider` / `setTestProviderLocation`，
  系统在写入时会**强制**把位置标记为模拟，客户端抹不掉（`Location.setMock` 反射调用会"成功"，
  但读回来仍是 `[模拟]`）。因此：
  - 如果目标 App 过滤模拟标记，免 root 方案没有解；
  - 如果目标 App 自己做网络定位（不读系统定位），同样没有解。
  定位诊断里的心跳数据就是为了先排除掉「其实是本应用被系统杀了」这一种情况，
  剩下的才能归因到上面两条。
- 版本号 1.2.0 → 1.2.5

---

## [1.2.0] - 2026-09-18

### 新增

- **验证页支持两种方式任选**：原来的「名字缩写」之外，新增「随机口算」——
  每次进入页面或点「换一题」都会生成一道新的两位数加减法，填答案即可通过；
  答错自动换一道，免得卡在同一题上反复试。
- 验证页顶部加方式切换胶囊，切换时清空错误提示。
- 口算只收数字输入，键盘为数字键盘。

### 说明

- 题目约束：加法是两个 11~49 的数（结果 22~98）；减法**保证被减数更大**，
  结果永远是 1~39 —— 出现负数答案会让人先怀疑自己算错了，而不是题目出得怪。
- ⚠️ **两道门只要有一道好过，整体就是好过的。** 算术题谁都会算，
  所以它**不提供任何防护**，只挡误触与随手乱点。要防人请用名字缩写方式。

### 变更

- 版本号 1.1.1 → 1.2.0

---

## [1.1.1] - 2026-09-18

### 修复

- **修复精简版启动即闪退**（1.1.0 引入的问题）。
  根因是 release 构建开着 R8 的**优化**：方法内联与类合并会破坏 Compose
  运行时对编译器生成调用结构的约定。改为 `-dontoptimize`（只裁剪、不优化）后，
  应用类结构与调试版一一对应（384 个 vs 381 个），静态验证也重新成立。

### 变更

- 精简版体积 1.9 MB → 2.5 MB，换回可验证性与稳定性。

---

## [1.1.0] - 2026-09-18 ⚠️ 已撤回

### 新增

- 首次提供经 R8 裁剪的「精简版」安装包，体积从 17.6 MB 降到 1.9 MB。
  体积大头是 `material-icons-extended`（85 MB 的 jar，几千个图标，
  本工程实际只用到十几个），调试版不做裁剪会把它们全部打进包。

### 已知问题

- **精简版启动即闪退**，已从 Releases 撤下（Release 与 tag 均已删除）。
- 教训：R8 的优化阶段会内联方法、合并类，**静态检查无法证明功能完整** ——
  当时在 dex 里搜 `androidx/compose/runtime/` 全是 0 次，
  分不清是「被裁掉了」还是「被改名了」。而这个包没有真机可验证。

---

## [1.0.0] - 2026-09-18

首个公开版本。Android 8.0+（minSdk 26），无需 Root。

### 功能

- **手绘轨迹**：按住屏幕拖动即可画出路线，松手成线，可分数笔续画；
  自动抽稀（Douglas-Peucker，2m 预过滤 + 6m 容差），几百个采样点压成十几个拐点
- **点选设点**：点地图加点、长按加点、点圆点删除
- **精确落点**：十字准星模式，拖动地图对准目标后落点
- **自定义速度**：2–25 km/h 滑杆，预设 6 / 8 / 10 / 12 / 15，实时显示配速
- **路线方式**：单程 / 环线刷圈 / 折返（到终点自动掉头）
- **目标距离**：跑够设定距离自动停止
- **轨迹抖动**：米级随机偏移，避免轨迹过于规整
- **路线预设**：保存 / 载入 / 重命名 / 删除，支持 JSON 导入导出
- **整笔撤销**：按「操作快照」记录，一笔 / 一次清空 / 一次载入都能一步撤销（最多 40 步）
- **后台运行**：前台服务 + 常驻通知，5 Hz 推送，息屏或切到其他应用仍继续
- **地点搜索**：Photon（Komoot 开源的 OSM 地理编码），免 API Key
- **按坐标输入**：直接填经纬度添加坐标点
- **底图**：仅 OpenStreetMap，默认使用返回 **512×512** 的高清镜像
  （同视野同字号下像素密度是 256px 瓦片的两倍）
- **连接诊断**：逐端点实测并显示**服务端真实返回的像素尺寸**，一键「测速并选最快」
- **离线模式**：只读本地缓存；可导入 .mbtiles / .sqlite / .zip 离线包
- **启动页**：自绘林间岔路矢量图 + 弗罗斯特《未选择的路》
- **进入口令**：启动页之后验证一次，通过后本机不再询问
- 小清新浅色主题（薄荷绿主色 + 天蓝辅助色 + 大圆角）

### 技术要点

- 定位走系统公开的 `LocationManager.addTestProvider / setTestProviderLocation`，
  需在开发者选项中把本应用设为「模拟位置信息应用」；
  **不修改、不 hook、不注入任何其他应用**
- 轨迹严格按 `速度 × 真实经过时间` 沿折线推进，调用间隔抖动不会造成速度偏差
- 距离用 WGS-84 近似球面公式（haversine），跑步尺度误差在米级
- 使用 WGS-84 坐标，与 GPS 一致，不存在 GCJ-02 偏移问题
